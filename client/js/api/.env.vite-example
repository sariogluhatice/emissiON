# =========================================================================
# VITE ORTAMI İÇİN GEREKLİ ORTAM DEĞİŞKENLERİ (ENVIRONMENT VARIABLES)
# =========================================================================
# İlerleyen haftalarda projenizin arayüzü Vite, React veya Webpack gibi
# bir derleyici (bundler) altyapısına geçirilirse;
# 
# 1. Bu dosyanın adını ".env" olarak değiştirin ve ana dizine alın.
# 2. client/js/api/env.js içerisindeki yorum satırını (import.meta.env) aktif edin.
# =========================================================================

# VITE_API_BASE_URL=http://localhost:3000/api
